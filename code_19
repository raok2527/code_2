#include<stdio.h>

  void display(*j,int n)
  {
    int i=1;
    while(i<=n)
    {
      printf("\n element no is %d",num[i]);
      printf("\n address is %u",&num[i]);
      i++;
      j++;
    }
  }
void main()
{
  int num[]={24,34,12,44,56,17};
  display(&num[0],6);
}
